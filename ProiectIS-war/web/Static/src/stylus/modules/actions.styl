.action {
    display: flex;
    padding-top: rem(14);
    justify-content: space-between;
    padding-bottom: rem(10);
    
    .btn {
        margin-right: rem(4);

        &:last-child {
            margin-right: 0;
        }
    }
}